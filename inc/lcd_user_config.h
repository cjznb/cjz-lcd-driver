/**
 * @file    lcd_user_config.h
 * @brief   LCD驱动用户配置文件
 * @note    用户需根据实际硬件和LCD屏幕修改本文件中的配置项。
 */

#ifndef LCD_USER_CONFIG_H_
#define LCD_USER_CONFIG_H_

#include <stdint.h>

/* ========================================================================
 * 一、LCD 驱动芯片选择（选择一个）
 * ======================================================================== */

#define LCD_DRIVER_CHIP_ST7789    1   /* ST7789 / ST7789VW (240×320) */
// #define LCD_DRIVER_CHIP_ILI9341   1   /* ILI9341 (240×320) */
// #define LCD_DRIVER_CHIP_ILI9488   1   /* ILI9488 (320×480) */
// #define LCD_DRIVER_CHIP_ST7735    1   /* ST7735 / ST7735S (128×160 / 128×128) */

/* ========================================================================
 * 二、LCD 接口类型选择（二选一）
 * ======================================================================== */

#define LCD_USE_SPI     1   /* 使用SPI接口（4线SPI：SCK/MOSI/DC/CS）*/
// #define LCD_USE_FSMC    1   /* 使用FSMC/8080并行接口 */

/* ========================================================================
 * 三、LCD 显示方向配置
 * ======================================================================== */

#define LCD_SWAP_COLOR_BYTES    0   /* 1=颜色字节交换（高低字节对调，某些屏幕需要）*/
#define LCD_SWAP_RGB            0   /* 1=RGB/BGR颜色通道交换 */
#define LCD_HORIZONTAL_FLIP     0   /* 1=水平翻转，0=正常 */
#define LCD_VERTICAL_FLIP       0   /* 1=垂直翻转，0=正常 */
#define LCD_COLOR_INVERSION     1   /* 1=颜色反转，0=不反转（通常ST7789等需要反转）*/

/* ========================================================================
 * 四、LCD 物理分辨率
 * ======================================================================== */

#define LCD_RAW_WIDTH   240      /* LCD 物理宽度（像素）*/
#define LCD_RAW_HEIGHT  320      /* LCD 物理高度（像素）*/

/* ========================================================================
 * 五、传输与缓冲区参数
 * ======================================================================== */

/** 使用 LCD_WriteBuffer 函数时每次传输的数据块大小 */
#define TRANSMIT_BLOCK_SIZE     ((size_t)(16 * 1024))   /* 16KB */

/** 创建缓冲区时的最大分块限制 */
#define MAX_BUFFER_SIZE         ((size_t)(16 * 1024))   /* 16KB */

#ifndef LCD_COLOR_INVERSION
#define LCD_COLOR_INVERSION     0
#endif

#endif /* LCD_USER_CONFIG_H_ */
